import Plugin from 'src/index';

describe('Test index of project', () => {
  it('Index should return all needed objects', () => {
  //   const plugin = new Plugin();
  //   plugin.init();
  //
  //   plugin.data.addComponent('id', plugin.data.definitions.components[0]);
  //   const files = plugin.__renderer.renderFiles();
  //   console.log(files);
  // });
});
